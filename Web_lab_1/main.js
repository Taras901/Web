const OPEN_CLASSNAME = "open";

const navLinks = document.getElementById("div_id");

function toggleMenu() {
    if (div_id.classList.contains(OPEN_CLASSNAME)) {
    div_id.classList.remove(OPEN_CLASSNAME);
    } else {
    div_id.classList.add(OPEN_CLASSNAME);
    }
}